#!/usr/bin/env bash
set -e
if [[ ! -d "/home/main/shared/personal/Projects/open-webui" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/main/shared/personal/Projects/open-webui")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/main/shared/personal/Projects/open-webui" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/main/shared/personal/Projects/open-webui/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/main/shared/personal/Projects/open-webui/.envrc" "/home/main/shared/personal/Projects/open-webui/.direnv"/*.rc
